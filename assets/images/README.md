Drop raster assets here (OG default image, favicon PNG fallback if you
want one beyond the SVG favicon, any photos). Nothing is required by
the boilerplate itself — every current image reference is SVG.

Referenced but not yet created:
- og-default.png (1200x630) — used as the fallback og:image on index.html
